{
  "name": "prompting-for-performance",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist -r https://x-access-token:${GITHUB_TOKEN}@github.com/pallavibiswas/prompting_for_performance.git --user \"github-actions-bot <github-actions-bot@users.noreply.github.com>\""
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.0"
  },
  "dependencies": {
    "marked": "^18.0.5"
  }
}
